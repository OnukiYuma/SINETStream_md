name: Python application

on: push

jobs:
    build:
        runs-on: ubuntu-latest

        steps:
            - name: Create LangPages
              run: exec mkdocs python scripts/multi_langpage.py
